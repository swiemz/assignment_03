# I feel like I'm getting better at these labs overall. This one especially felt pretty comfortable, as I understood the directions and most of the jargon, and I could actually navigate the Streamlit stuff on my own: setting the title, importing the functions from the module, running the app in the terminal, and pulling it up in the browser. It helped that I already had a good idea of what the finished app was supposed to look like since it was similar to stuff we did in class. Where I still trip up is the actual code logic. I used the AI chatbot to get from the instructions to working code, then read through it line by line to figure out what each function and parameter was actually doing, especially after seeing it run in the app. I got confused about how to decode the lines of an uploaded file, and I wasn't sure how I'd debug the contents if the input to the app wasn't what I expected. I don't think I'd know where to set a breakpoint for that case. I also kept losing track of which parts of the code needed to be nested inside other parts to actually run. One thing I was proud of: I fixed the testing errors myself this time without needing the chatbot, like the process_file vs process_files naming mistake. That felt like actual progress. In the future, I need to work on my code writing and understanding. next lab, I want to write the first draft of the code myself before bringing in the chatbot, even if it's wrong, so I'm forced to think through the function structure first. I also want to practice using the debugger on a file-input case specifically, since that's where I got stuck this time. I will set a breakpoint right after the file is read in and stepping through to see what the data actually looks like before it hits the rest of the function.